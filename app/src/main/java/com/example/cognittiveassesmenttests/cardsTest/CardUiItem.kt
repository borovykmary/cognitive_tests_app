package com.example.cognittiveassesmenttests.cardsTest

/**
 * A data class that represents a card in the Cards test.
 * It holds the card's properties, such as color, number, and shape.
 */
data class CardUiItem(
    val resource: String,
    val color: String,
    val number: Int,
    val shape: String
)
