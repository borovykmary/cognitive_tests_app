package com.example.cognittiveassesmenttests.cardsTest

/*import androidx.compose.runtime.*
import androidx.compose.ui.graphics.Color
import androidx.lifecycle.ViewModel

class CardViewModel :ViewModel() {

    var isCurrentlyDragging by mutableStateOf(false)
        private set

    var items by mutableStateOf(emptyList<CardUiItem>())
        private set

    var addedCards = mutableStateListOf<CardUiItem>()
        private set

    init {
        items = listOf(
            CardUiItem("card_red_2_square", "red", 2, "square")
        )
    }

    fun startDragging() {
        isCurrentlyDragging = true
    }

    fun stopDragging() {
        isCurrentlyDragging = false
    }

    fun addCard(cardUiItem: CardUiItem) {
        println("Added Card")
        addedCards.add(cardUiItem)
    }
}
*/
