package com.example.cognittiveassesmenttests.dataClasses

/**
 * A data class that represents a record of a CARD test.
 * It holds the test's properties, such as date, score, and time.
 */
data class TestRecordCARD(
    val testDate: String,
    val testScore: String,
    val testTime: String
)
